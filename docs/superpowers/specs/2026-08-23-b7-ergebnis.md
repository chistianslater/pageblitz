# B7-Ergebnis: GMB-Wahrheit, vollständige Erstgenerierung, Zeitmaschine, Pack-Feinschliff

**Datum:** 2026-08-23 · **Branch:** `onboarding-v2` · **Stand:** `c172493 (+ Doku-Commit)` · **Plan:** `docs/superpowers/plans/2026-08-23-onboarding-v2-b7-gmb-generierung.md` · **Spec:** `docs/superpowers/specs/2026-08-23-b7-gmb-generierung-design.md` (freigegeben „Das passt", §5 wie empfohlen) · **Vorlauf:** B6-Ergebnis, Prod-Diagnose Website 491 „SCHAU & HORCH".

## 1. Ergebnis je Task

| Task | Commit(s) | Kern | Review |
|---|---|---|---|
| 1. GMB-Tiefenabruf | `4e84742`, `88e756a` | `server/gmb/{details,category,address}.ts`: Details mit 12 Feldern + Places-v1 `primaryTypeDisplayName` (Feature-Detection; generische Namen zählen nicht), DE-Mapping ~68 Types, Kategorie **nie** aus dem Firmennamen (`null` statt Query-Fallback), Adress-Parser, idempotente Persistenz (Reviews mit Feld-Picking, keine Profil-URLs), Migration 0030 (`editorialSummary`, additiv), `selfService.start`-Abruf fire-and-forget | Approve m. F. → gefixt |
| 2. Website-Crawl | `de34f58`, `ebf6b0f` | `server/gmb/siteCrawl.ts`: robots-konformer Einzelseiten-Crawl (10 s/200 kB, Redirects ≤ 3 same-host, SSRF-gehärtet inkl. **IPv6-mapped/NAT64** nach Review-Critical, Wildcard-Disallows konservativ, Kappungen), `facts.existingSite` als gerahmter Fremdinhalt im Prompt | Änderungen erforderlich → Fixwelle, danach ok |
| 3. Faktentreue Generierung | `68381f4`, `b595e0a` | Stadt/PLZ/Straße aus GMB-Adresse; 6–8 Sektionen: Testimonials aus echten Reviews (deterministisch), GMB-Foto-Galerie, FAQ, Öffnungszeiten; `factGuard` (Stadt-Ersetzung + Branchen-Retry ×1); **Key-Leak zu**: GMB-Fotos nur noch als R2-URLs (`mirrorGmbPhotosToR2`); `website.regenerate` gleiche Pipeline; **`schauHorch.e2e.test.ts`** (realer Fehlerfall: „Optik und Akustik … München" → Bocholt, 7 Sektionen, kein Key) | Approve m. F. → in Fixwelle |
| 4. Zeitmaschinen-UX | `44b9806` | Interim-Doc nach Bild-Phase (schema-valide, Restore bei Fehlern), Studio: Pack-Skeleton → Zwischenstand → final mit Sektions-Reveal (nur Preview), Fortschrittsbalken läuft kontinuierlich (rAF, reduced-motion-Variante), `PB_V2_PHASE_DELAY_MS` (non-prod) | Approve m. F. → in Fixwelle |
| 5. Kategorie-Rückfrage | `7b60a6b` | `needsCategory`-Zustand, Mutation `setCategory`, Studio-Schritt „Was macht dein Betrieb?" (ARIA-Combobox aus `shared/gmbCategories`), Auto-Start gesperrt bis Kategorie da | Approve |
| Koordinator-Fixwelle | `eafd1f1`…`b97b887` (7 Commits) | Generierungs-Gate (Checkout/Patches BAD_REQUEST während `pending|processing`), `ensureGeneration`-Retry via Interim-Marker, `getPhotoSources` ohne Google-Key, Foto-Timeout 12 s + 3er-Pool, Restore-Lücke, Prompt-Rahmung editorialSummary, GenerationScreen-Minors, CategoryStep-Blur, Mobile-a11y-Tests (390px, 3 Packs), morgenlicht `accent-text` #256A64, stale Studio-Baselines | — |
| 6. Pack-Feinschliff | `131dcec` (W0), `cc7a9a2` (W1), `644aa10` (W2), `4ec883b` (W3), `e37653e`, `c172493` | Regelwerk R1–R6 (`…b7-feinschliff-regeln.md`) + priorisierte Fixliste (42 Screens gesichtet); Welle 0 = Querfixes (sticky Nav in allen 14, Über-uns-Zweispalter, Öffnungszeiten-Block, Unterseiten-Reihenfolge, Mobil-Nav, disjunkte Demo-Bilder + 2 neue Assets); Wellen 1–3 = alle P1/P2 je Pack (u. a. schimmer-Bildanker, patina-Bewertungszeile, werkbank-Diagonal-Clip, gusto-Teaser/Kreis, kanzlei/fundament-`span`-Root-Cause, landgut-Bogenfoto, zunft-Stempel, Footer-Querfix); Reviews W0–W3 mit 4 Important-Funden → alle gefixt (patina-Orphan-Guard, Mobile-a11y, schimmer-Fallback, salon-noir-Signatur kontrolliert zurück) | W0 Warning→gefixt; W1+2/W3 Approve |
| StartPage | `2534958` | `/start` im Studio-Look (alle 3 Schritte), toter Dark-Pfad raus, Preis aus `shared/pricing.ts` | Sichtprüfung |
| 7. Abschluss | Doku-Commit | BETRIEB-V2 (§3 Migration 0030, §6a GMB/Generierung/Zeitmaschine), dieses Dokument, Spec-Stand; Katalog-Artifact aktualisiert (Stand Wellen 0–3) | — |

## 2. Gates — Stand Abschluss

| Gate | Ergebnis |
|---|---|
| `tsc --noEmit` | 0 |
| `vitest run` | 110 Testdateien grün, 1.38x Tests passed; 3 Fail-Dateien (bekannte Env-Fails: 2× resend, `auth.logout`/`pageblitz` Stripe-Env) |
| `npm run build` | grün |
| Playwright `dev` alle Specs | 162/162 |
| Playwright `dev` 2. Lauf (studio/landing/a11y) | 47/47 |
| Playwright `prod` | 9/9 (2×) |

## 3. Erfolgskriterien Spec §4

- „SCHAU & HORCH" generiert richtig (Branche aus GMB/Website statt Name, Bocholt statt München, 6–8 Sektionen inkl. 13×5,0-Reviews + Fotos, kein API-Key in Bild-URLs) — **✅ als E2E-Test** (`schauHorch.e2e.test.ts`, gemockte GMB-Daten des echten Falls + absichtlich falsche LLM-Antwort); echte Prod-Regenerierung von Website 491 folgt beim Deploy (§5).
- Vorschau < 5 s sichtbar (Skeleton), Sektionen erscheinen fortlaufend, Balken steht nie — **✅** (Playwright-Szenario mit Phasen-Bremse; `PB_LLM_MOCK`).
- Feintuning: **alle P1/P2 der Fixliste umgesetzt oder begründet geschlossen** (Restliste in `welle-3-report.md`: marktplatz-Hero-Musikmotiv (Asset-Pool), Q7 Nav-Verdichtung (P3), atelier-Illustrations-Ruling); Katalog aktualisiert — Abnahme durch User offen.

## 4. Rulings und Abweichungen

- Kategorie-Kette liefert `null` statt raten; Alt-Fallback „Dienstleistung" bleibt nur als letztes Netz im Job (Legacy/Admin-Pfade).
- atelier bleibt Illustrations-Welt (Verfassung schlägt Kritiker-Empfehlung „Fotos").
- salon-noir-Signatur `headline-image-overlap` kontrolliert wiederhergestellt (64 px, ≥ 1160 px) statt Verfassung zu ändern.
- `outreachPipeline.ts` nutzt weiter die alte Kategorie-Logik (eigene Kopie) — dokumentierte Drift, Backlog.
- `getPhotoSources` respiegelt ohne placeId-Cache (neue R2-Objekte möglich) — bewusst, Backlog.
- DNS-Rebinding-Restfenster des Crawls dokumentiert-akzeptiert (Fix wäre IP-Pinning).
- Guard-Grenzen: Adjektiv-/Genitiv-Städte („Münchner") ungefiltert — akzeptiert.
- Zwei-Tab-Race bei `setCategory` (Last-Write-Wins auf `businesses.category`, UI bleibt korrekt) — akzeptiert.

## 5. Prod-Deploy (nach Merge mit Freigabe)

1. Deploy wie gewohnt; Migration 0030 (`mysql -uroot -p pageblitz < drizzle/0030_business_editorial_summary.sql`, additiv).
2. **Prod-E2E:** „Schau und Horch" über `/start` neu anlegen/regenerieren → Branche/Stadt/Sektionen prüfen; Website 491 (alter Stand mit Key-URLs + falscher Branche) dabei ersetzen oder löschen; Testwebsite 490 löschen (offen seit B6).
3. **iOS-Safari-Smoke** (Welle-0-Review): sticky Nav auf 2–3 Demo-Packs am echten iPhone prüfen (Playwright deckt nur Chromium).
4. Weiterhin offen (User): 8 Stripe-Preise `STRIPE_PRICE_ADDON_*` anlegen/eintragen (seit B6).

## 6. Offen nach B7 (Backlog)

Feintuning-Nachwelle nach User-Markierungen im Katalog; marktplatz-Hero-Musikmotiv (1 Asset generieren); Q7 Nav-Verdichtung (P3); outreachPipeline-Kategorie-Konsolidierung; placeId-Cache für Foto-Spiegelung; Job-Watchdog für hängende `processing`-Jobs (vorbestehend); `getPhotoSources`-Respiegelung härten; werkbank-Galerie-Captions bei LLM-Alt-Texten beobachten.
